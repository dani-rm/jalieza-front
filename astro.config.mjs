// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://jalieza.com.mx",
  base: "/",
  trailingSlash: "ignore",
  integrations: [],
});
